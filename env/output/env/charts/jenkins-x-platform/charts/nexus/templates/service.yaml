---
# Source: env/charts/jenkins-x-platform/charts/nexus/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nexus
  labels:
    app: nexus
    chart: nexus-0.1.7
    release: jx-app-datadog
    heritage: Tiller
  annotations:
    fabric8.io/expose: "true"
    fabric8.io/ingress.annotations: 'kubernetes.io/ingress.class: nginx'
    
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
      name: nexus
  selector:
    app: nexus
    release: jx-app-datadog