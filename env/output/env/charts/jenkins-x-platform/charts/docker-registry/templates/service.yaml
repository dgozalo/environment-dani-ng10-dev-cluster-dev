---
# Source: env/charts/jenkins-x-platform/charts/docker-registry/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: jx-app-datadog-docker-registry
  labels:
    app: docker-registry
    chart: docker-registry-1.5.0
    release: jx-app-datadog
    heritage: Tiller
  annotations:
    fabric8.io/expose: "true"
    fabric8.io/ingress.annotations: |-
      kubernetes.io/ingress.class: nginx
      nginx.ingress.kubernetes.io/proxy-body-size: 2g
    
spec:
  type: ClusterIP
  ports:
    - port: 5000
      protocol: TCP
      name: registry
      targetPort: 5000
  selector:
    app: docker-registry
    release: jx-app-datadog
